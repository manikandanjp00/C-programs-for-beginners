#includestdio.h
int main()
{
    int n=5;
    int i,j;
    for(i=1;i=n;i++)
    {
        for(j=1;j=2n;j++)
        {
            if(in-j+1 && i+nj)
                printf(   );
            else
                printf( );
        }
        printf(n);
    }
    for(i=1;i=n;i++)
    {
        for(j=1;j=2n;j++)
        {
            if(ij && 2n-i=j)
                printf(   );
            else
                printf( );
        }
        printf(n);
    }
    return 0;
}